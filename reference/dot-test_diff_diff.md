# Test if differences of differences are significant

Test if differences of differences are significant

## Usage

``` r
.test_diff_diff(
  dataframe_a,
  dataframe_b,
  by,
  diff = c("diff"),
  std_err = c("std.error"),
  df = c("df"),
  suffix_a = ".site",
  suffix_b = ".protein"
)
```
