# Write the Example Result Table as a Workbook

The compute functions take a workbook path, because that is what the
pipeline has. The example data is an R object, so it is written out once
per session to give the standalone path the same input shape.

## Usage

``` r
example_results_workbook()
```

## Value

Path to the workbook.
