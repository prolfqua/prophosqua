% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_enrichment.R
\name{compute_mea}
\alias{compute_mea}
\title{Collect the Motif Enrichment Results of One Analysis}
\usage{
compute_mea(kinaselib_dir)
}
\arguments{
\item{kinaselib_dir}{Directory holding the `mea_*.csv` files.}
}
\value{
A list with `mea_clean`, its per-contrast `summary_df`, and
  `has_results`.
}
\description{
Reads the per-contrast `mea_*.csv` files the kinase-library tool wrote and
brings them onto the column names the enrichment reports share. Unlike the
two GSEA analyses this does no testing of its own — the enrichment already
happened, one contrast per file.
}
\examples{
# Needs the mea_*.csv files of a pipeline run.
\dontrun{
res <- compute_mea("PTM_DPA/KinaseLib")
res$summary_df
}
}
