% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_kinaselib.R
\name{prep_kinaselib_inputs}
\alias{prep_kinaselib_inputs}
\title{Prepare Kinase-Library Inputs from the Combined PTM Results}
\usage{
prep_kinaselib_inputs(xlsx_file, output_dir, analysis_type, sheet, stat_column)
}
\arguments{
\item{xlsx_file}{The combined `PTM_results.xlsx`.}

\item{output_dir}{Where to write, normally the analysis `KinaseLib` directory.}

\item{analysis_type}{`DPA`, `DPU` or `CF`; names the output files.}

\item{sheet}{Sheet of `xlsx_file` to read.}

\item{stat_column}{Column to rank the sites on.}
}
\value{
Invisibly, a character vector of the files written.
}
\description{
Writes the two file kinds the kinase-library tool consumes: one list of
unique sequence windows for motif scanning, and one ranked file per contrast
for motif enrichment analysis.
}
\details{
The workbook, sheet and ranking statistic are the same ones PTM-SEA and the
KinaseLib GSEA read, so all three enrichment analyses rank the sites
identically and their results can be compared.
}
\examples{
# Needs a combined workbook from a pipeline run.
\dontrun{
prep_kinaselib_inputs(
  "PTM_results.xlsx", "PTM_DPA/KinaseLib",
  analysis_type = "DPA", sheet = "DPA", stat_column = "statistic.site"
)
}
}
