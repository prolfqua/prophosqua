% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dea_io.R
\name{canonicalize_dea_sample_column}
\alias{canonicalize_dea_sample_column}
\title{Canonicalize the Sample Column in Normalized DEA Data}
\usage{
canonicalize_dea_sample_column(data, yaml_file, canonical_name = "Name")
}
\arguments{
\item{data}{Normalized DEA data}

\item{yaml_file}{Path to its `lfqdata.yaml` file}

\item{canonical_name}{Output column name}
}
\value{
`data` with its declared sample column renamed
}
\description{
Renames the sample column a DEA run declares to a fixed name, so that data
from runs using different schemas can be joined.
}
\examples{
yaml_file <- file.path(tempdir(), "lfqdata.yaml")
writeLines("sample_name: sampleName", yaml_file)
data <- data.frame(sampleName = c("a", "b"), abundance = c(1, 2))

canonicalize_dea_sample_column(data, yaml_file)
}
