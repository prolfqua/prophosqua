% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gsea_result_json.R
\name{mea_gsea_result_data}
\alias{mea_gsea_result_data}
\title{Convert Collected MEA Results to the GSEAResult Structure}
\usage{
mea_gsea_result_data(mea_clean, ranks, term2gene, category = "MEA")
}
\arguments{
\item{mea_clean}{The `mea_clean` table of [compute_mea()].}

\item{ranks}{Named list of ranked vectors, one per contrast.}

\item{term2gene}{Motif-scan assignment with `term` and `gene` columns.}

\item{category}{Category name recorded on every term.}
}
\value{
A GSEAResult as a named list with `data` and `rank_lists`.
}
\description{
The motif enrichment ran outside R (kinase-library CLI), so its inputs are
reassembled: the ranked lists come from the `.rnk` files the pipeline wrote
(see [read_mea_ranks()]), the full set membership from the `term2gene.csv`
motif-scan assignment, and the leading edge from the `Leading substrates`
column of the collected results.
}
\examples{
\dontrun{
res <- compute_mea("PTM_DPA/KinaseLib")
ranks <- read_mea_ranks("PTM_DPA/KinaseLib")
term2gene <- read.csv("PTM_DPA/KinaseLib/term2gene.csv")
json_data <- mea_gsea_result_data(res$mea_clean, ranks, term2gene)
}
}
